class Solution:
    def reverse(self, arr: list, n: int) -> None:
        def helper(l,r):
            if l>=r:
                return
            arr[l],arr[r] =arr[r],arr[l]
            helper(l+1,r-1)
        helper(0,n-1)
