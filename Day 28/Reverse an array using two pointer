arr=list(map(int,input("Enter the space seperated elements:").split()))
n=len(arr)
print("Array before reversing:",arr)
left=0
right=n-1
while left<right:
    arr[left],arr[right]=arr[right],arr[left]
    left+=1
    right-=1
print("Array after reversing:",arr)
