class Solution:
    def upperBound(self, nums, x):
        n=len(nums)
        l,h=0,n-1
        ans=n
        while l<=h:
            mid=l+(h-l)//2
            if nums[mid] > x:
                ans=mid
                h=mid-1
            else:
                l=mid+1
        return ans
        
