def is_armstrong(num):
    digits = len(str(num))
    total = 0
    temp=num 
    while temp:
        digit = temp % 10
        total += digit ** digits
        temp //= 10
    return total == num
    
num= int(input().strip())
if is_armstrong(num):
    print("True")
else:
    print("False")
