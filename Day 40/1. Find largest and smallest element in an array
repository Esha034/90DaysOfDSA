nums=list(map(int,input().split()))
if not nums:
    print("Array is empty")
else:
    min_val=nums[0]
    max_val=nums[0]
    for i in range(1,len(nums)):
        if nums[i]>max_val:
            max_val=nums[i]
        elif nums[i]<min_val:
            min_val=nums[i]
    print(min_val)
    print(max_val)
