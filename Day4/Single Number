class Solution:
    def singleNumber(self, nums: List[int]) -> int:
        n=len(nums)
        res=0
        for num in nums:
            res^=num
        return res

















        
# # More better 


#         freq = {}

#         # Store frequency
#         for num in nums:
#             freq[num] = freq.get(num, 0) + 1

#         # Find unique element
#         for key, val in freq.items():
#             if val == 1:
#                 return key

# #Better
#         maxElement = max(nums)
#         hashArr = [0] * (maxElement + 1)

#         # Count frequency
#         for num in nums:
#             hashArr[num] += 1

#         # Find element with freq = 1
#         for num in nums:
#             if hashArr[num] == 1:
#                 return num



# #Bruteforce
#         n=len(nums)
#         for i in range(n):
#             count=0
#             for j in range(n):
#                 if nums[i]==nums[j+1]:
#                     count+=1
#             if count==1:
#                 return nums[i]  
