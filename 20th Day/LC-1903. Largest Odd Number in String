class Solution:
    def largestOddNumber(self, s: str) -> str:
        n=len(s)
        for i in range((n)-1,-1,-1):
            if int(s[i])%2==1:
                return s[0:i+1]
        
        return ""
