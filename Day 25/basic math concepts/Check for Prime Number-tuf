class Solution:
    def isPrime(self, n):
        # Edge case
        if n < 2:
            return False

        # Loop from 2 to n-1
        for i in range(2, n):

            # Check if i is a divisor
            if n % i == 0:
                return False

        # Return true as the number is prime
        return True

